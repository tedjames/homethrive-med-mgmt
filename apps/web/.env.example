# Web Environment Variables
# Run 'pnpm dev:setup' to configure, or copy to .env and fill in values

# API URL
# Local development:
VITE_API_URL=http://localhost:3200
# Production (set by deploy script):
# VITE_API_URL=https://xxx.execute-api.us-east-1.amazonaws.com

# Clerk Authentication (publishable key only - no secret needed for CSR)
# Get the publishable key from Clerk dashboard (Frontend API Keys)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME
